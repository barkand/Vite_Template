export const enum DeviceType {
  Web = "web",
  Mobile = "mobile",
}

export const enum ThemeType {
  Dark = "dark",
  Light = "light",
}

export const enum ThemeColorType {
  Primary = "primary",
  Secondary = "secondary",
}

export enum LanguagesType {
  En = "en",
  Fa = "fa",
}

export enum RoutesType {
  Home = "/",
  Profile = "/profile",
  List = "/List",
  Item = "/Item",
  NotFoundPage = "*",
}

export enum EditType {
  None,
  Editing,
  Edited,
  Error,
}

export const enum StatusType {
  Error = "error",
  Success = "success",
  Warning = "warning",
  Info = "info",
}

export enum AuthType {
  Wallet = "wallet",
  Mobile = "mobile",
  Mail = "mail",
}
