import { ThemeTypeEnum } from "@/core/constant";

const theme: any = {
  dark: {
    name: ThemeTypeEnum.Dark,
    isDark: true,
  },
  light: {
    name: ThemeTypeEnum.Light,
    isDark: false,
  },
};

export default theme;
